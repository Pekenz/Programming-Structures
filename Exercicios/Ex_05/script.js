let vel = prompt("Digite a velocidade em que está na via: ");

if(vel >= 80) {
    console.log("Você está acima da velocidade permitida na via, multado!");
}else {
    console.log("Você está dentro da velocidade, parabéns!");
}