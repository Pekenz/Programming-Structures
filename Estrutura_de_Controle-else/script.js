//Else é utilizado para executar outro bloco de código, caso a instrução do if não seja atendida, ou seja, SE NÃO EXECUTAR o bloco if, EXECUTE o bloco else, ex:

let nome = 'Pedro'

if (nome == 'Rafael') {
    console.log('Bem vindo rafael');
} else {
    console.log('O seu nome não é Rafael')
}