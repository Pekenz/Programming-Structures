//A estrutura 'For' é semelhante a 'While', porém é bem mais utilizada e mais recomendada por programadores experientes, a sintaxe sendo:

for(let num = 0; num < 10; num = num + 1) {
    console.log('O numero é: ' + (num + 1))
}